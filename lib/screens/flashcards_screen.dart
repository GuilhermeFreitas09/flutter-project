import 'package:flutter/material.dart';

class MyFlashCardsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

        //body: Text("Flashcard"),
        );
  }
}
